# 原神角色展示网站 — 实施计划

## Context

用户需要一个原神角色信息网站，用户可以点击角色卡片查看详细信息（故事、性格、擅长事物）。纯前端实现，无需后端。

## 技术选型

- **纯 HTML/CSS/JS**：无框架依赖，直接在浏览器打开 `index.html` 即可运行
- **数据存储**：独立 JS 文件存放角色 JSON 数据
- **设计风格**：原神风格（深色背景、金色点缀、幻想风排版）

## 文件结构

```
d:/Claude Code写东西测试专用/
├── index.html          # 主页面：角色卡片网格 + 筛选栏 + 模态详情弹窗
├── css/
│   └── style.css       # 全部样式（原神暗金主题）
├── js/
│   ├── data.js         # 角色数据（20+ 位角色，含姓名/元素/地区/故事/性格/擅长）
│   └── main.js         # 交互逻辑（渲染卡片、筛选、搜索、弹窗）
```

## 实现步骤

### 步骤 1：创建目录结构和 data.js

- 创建 `css/` 和 `js/` 子目录
- 在 `js/data.js` 中定义角色数组，每位角色包含：
  - `id`、`name`（中英文）、`element`（元素）、`region`（地区/国家）
  - `rarity`（4星/5星）、`weapon`（武器类型）
  - `story`（角色背景故事）
  - `personality`（性格描述）
  - `specialties`（擅长之事，数组形式）
- 收录 20-24 位角色，覆盖蒙德、璃月、稻妻、须弥、枫丹等地区及各元素

### 步骤 2：创建 index.html（页面结构）

- 页面头部：标题 + 副标题
- 筛选栏：按元素、按地区、搜索框
- 角色卡片网格：CSS Grid 响应式布局
- 角色详情模态弹窗模板
- 页脚

### 步骤 3：创建 style.css（原神风格）

- 深色渐变背景（深蓝黑 + 星空感）
- 金色点缀，用于标题、边框、高亮
- 卡牌式角色 card，含悬浮效果
- 模态弹窗：深色玻璃态，金色边框
- 响应式布局：大屏 4 列，中屏 3 列，小屏 2 列，手机 1 列

### 步骤 4：创建 main.js（交互逻辑）

- 页面加载时渲染全部角色卡片
- 筛选功能：按元素 / 地区筛选，可组合
- 搜索功能：实时过滤角色名称
- 点击卡片：打开模态弹窗，展示故事、性格、擅长之事
- 关闭弹窗：X 按钮 / 点击遮罩 / 按 ESC

### 步骤 5：测试验证

- 浏览器打开 `index.html`，确认所有功能正常
- 测试筛选、搜索、弹窗、响应式
- git commit 保存代码

## 角色数据覆盖（24位）

| 元素 | 角色 |
|------|------|
| 火 | 迪卢克、胡桃、班尼特 |
| 水 | 达达利亚、芙宁娜、行秋 |
| 风 | 温迪、魈、枫原万叶 |
| 雷 | 雷电将军、八重神子、刻晴 |
| 冰 | 甘雨、神里绫华、七七 |
| 草 | 纳西妲、艾尔海森、提纳里 |
| 岩 | 钟离、阿贝多、荒泷一斗 |
| — | 主角(空)、散兵 |
